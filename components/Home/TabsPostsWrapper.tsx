"use client";
import { Profile } from '@/app/types/entities';

const TabsPostsWrapper = ({ user }: { user: Profile | null }) => {
    return (
        <>
        </>
    );
};

export default TabsPostsWrapper;